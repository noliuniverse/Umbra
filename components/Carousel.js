"use client";
import styles from "@/app/globals.css"

import { useEffect, useState, useRef } from "react"


const Carousel = ({interval, bootstrap}) => {
    const INTERVAL_VAL = 3000;

  const [index, setIndex] = useState(0);




}

export default Carousel;